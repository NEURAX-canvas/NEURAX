fastapi==0.110.0
uvicorn[standard]==0.27.1
httpx==0.27.0
pydantic>=2.10.0,<3.0.0
python-dotenv>=1.0.0

# LLM providers — versions pinned for cross-compatibility. Mistral, Fireworks,
# DeepSeek and GLM all speak the OpenAI chat-completions HTTP shape natively,
# so langchain-openai (above) talks to them directly with the right base_url
# — no separate package needed for those four (langchain-mistralai exists but
# is deliberately not used here). Google/Gemini's native API is not
# OpenAI-shaped, so it needs its own client.
langchain-core==0.2.43
langchain-openai==0.1.22
langchain-anthropic==0.1.23
langchain-google-genai==1.0.10
google-generativeai>=0.7.0,<0.8.0

# Step-by-step agentic loop (agent_graph.py). Pinned to the 0.2.x line
# deliberately: langgraph>=0.3 (and its default langgraph-checkpoint/
# langgraph-sdk resolved by an unpinned `pip install langgraph`, which pulls
# langgraph 1.x) drags langchain-core up to a 1.x release, which breaks every
# provider package above (all pinned against langchain-core<0.3.0). Verified
# this exact trio installs clean against langchain-core==0.2.43 with no
# resolver conflicts — bumping langgraph means re-verifying the whole
# provider stack together, not just this one line.
langgraph==0.2.60
langgraph-checkpoint==2.0.24
langgraph-sdk==0.1.51
